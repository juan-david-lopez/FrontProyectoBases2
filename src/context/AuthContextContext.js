import { createContext } from 'react';

/**
 * Contexto de autenticación
 * Provee información del usuario autenticado y funciones de autenticación
 */
export const AuthContext = createContext(null);
